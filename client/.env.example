# Zero-cost default: leave unset — client loads /data/products.json statically.
# Optional Express API (server/): uncomment and point at your API origin.
# VITE_API_BASE_URL=http://localhost:3000/api
